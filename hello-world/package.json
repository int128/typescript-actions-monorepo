{
  "private": true,
  "name": "hello-world",
  "version": "0.0.0",
  "scripts": {
    "check": "biome check",
    "build": "ncc build --source-map --license licenses.txt src/main.ts",
    "test": "vitest"
  },
  "type": "module",
  "dependencies": {
    "@actions/core": "2.0.2"
  }
}
