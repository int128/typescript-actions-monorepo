{
  "private": true,
  "name": "hello-world",
  "version": "0.0.0",
  "scripts": {
    "format": "prettier --write **/*.ts",
    "lint": "eslint .",
    "build": "ncc build --source-map --license licenses.txt src/main.ts",
    "test": "jest"
  },
  "dependencies": {
    "@actions/core": "1.10.1"
  }
}
